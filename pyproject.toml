[project]
name = "corretor-vistoria"
version = "0.1.0"
description = "Aplicação desktop offline de pós-processamento de transcrições de vistoria (CloudSLIM)."
requires-python = ">=3.10"
dependencies = [
    "PySide6",
]

[tool.setuptools]
packages = ["app", "engine", "tools"]
